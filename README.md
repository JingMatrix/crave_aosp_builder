An empty manifest file for the `LOCAL_MANIFEST` parameter of CI build.

In the future, could be modified to build [custom kernels of Pixel](https://source.android.com/docs/setup/build/building-pixel-kernels).
